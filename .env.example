# SQLite for user profiles (SQLAlchemy)
SQLITE_DB_PATH=./agent_memory.db

# SQLite for LangGraph checkpoints (conversation thread persistence)
CHECKPOINT_DB_PATH=./checkpoints.db

# (Optional) anything your tools need, e.g. API keys
# OPENAI_API_KEY=...